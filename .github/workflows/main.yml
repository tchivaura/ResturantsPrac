name: restaurants-prac

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2

      - name: Setup .NET Core SDK
        uses: actions/setup-dotnet@v4.1.0
        with:
          dotnet-version: 8.0.x

      - name: Restore dependencies
        run: dotnet restore ./tchivaura/ResturantsPrac

      - name: Build
        run: dotnet build --no-restore ./tchivaura/ResturantsPrac

      - name: Test
        run: dotnet test ./tchivaura/ResturantsPrac
